#include <string>
#include <iostream>
#include <map>
#include <unordered_map>
#include <thread>
#include <list>
#include <chrono>

#include <unistd.h>
#include <execinfo.h>
#include <signal.h>
#include <math.h>

// Semantic Version
#define MAJOR (0)
#define MINOR (1)
#define REVISION (0)

// Milliseconds to second
#define MS (1000)

// Default TTL (seconds)
#define DEFAULT_TTL (60)

// Status
#define OK (0)
#define ERROR (1)

// Debug
#define DEBUG (0)
